// 변수는 var
// 상수는 let

// 변수 뒤에 타입을 명시해도 되고 안해도 된다.
// 타입을 명시하지 않으면 타입 추론을 통해 타입을 결정한다.
// 타입 추론은 변수를 선언할 때 초기화를 해야 한다.
// 변수를 선언할 때 초기화를 하지 않으면 타입 추론이 불가능하다.
// 변수를 선언할 때 초기화를 하지 않으면 타입을 명시해야 한다.
// 변수를 선언할 때 초기화를 하지 않으면 에러가 발생한다.

var str : String = "Hello";
let name : String = "Swift";